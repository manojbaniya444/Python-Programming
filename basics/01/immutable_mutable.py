# Immutable and mutable in python
# Immutable: int, float, str, tuple
# Mutable: list, dict, set

# in immutable objects, the value of the object cannot be changed the memory location of the object can be changed it just creates a new object and refer to it.

# in mutable objects, the value of the object can be changed and the same memory location is taken by the object.

x = 10
y = x

x = 15


username = "user123"
username = "user456"
